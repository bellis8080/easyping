# Story 1.1: Project Setup & Monorepo Infrastructure

## Status
Done

## Story

**As a** developer,
**I want** a fully configured monorepo with Next.js, TypeScript, and build tools,
**so that** I have a clean foundation to build features efficiently.

## Acceptance Criteria

1. Monorepo initialized with pnpm workspaces and Turborepo
2. Next.js 14+ app created in `apps/web` using App Router and TypeScript strict mode
3. Packages created for `database`, `ai`, `ui`, and `types` with proper package.json configuration
4. ESLint, Prettier, and TypeScript configured across all packages
5. Husky + lint-staged configured for pre-commit hooks (lint and type-check)
6. GitHub Actions CI/CD pipeline runs lint, type-check, and build on every PR
7. README with quickstart instructions for local development
8. `.env.example` file with required environment variables documented
9. **Performance budgets and optimization guidelines established:**
   - **Bundle size budget:** Main JavaScript bundle <500KB gzipped (enforced via Next.js build warnings)
   - **Page load budget:** Time to Interactive (TTI) <2 seconds on 4G connection
   - **API response budget:** 95th percentile <500ms for database queries, <2s for API endpoints
   - **Image optimization:** All uploaded images auto-compressed (max 1920px width, JPEG quality 85%, WebP format preferred)
   - **Asset optimization:** SVG icons minified, CSS purged via Tailwind, unused imports removed
   - **Bundle analyzer:** `@next/bundle-analyzer` configured to run on builds
   - **Performance monitoring:** Lighthouse CI configured to fail PR if scores drop below thresholds (Performance: 90, Accessibility: 95)
10. **Coding standards documented:**
    - TypeScript strict mode enforced (no `any` types without justification)
    - Component naming: PascalCase for components, camelCase for utilities
    - File structure: co-locate tests with source files (`*.test.ts` next to `*.ts`)
    - Import organization: external libs → internal packages → relative imports
    - ESLint rules enforce: no unused vars, prefer const, consistent return types

## Tasks / Subtasks

### Task 1: Initialize Monorepo Structure (AC: 1, 3)
- [x] Create project root directory `easyping/`
- [x] Initialize git repository with `git init`
- [x] Create `pnpm-workspace.yaml` with workspace configuration
- [x] Create root `package.json` with workspace scripts
- [x] Install Turborepo as dev dependency: `pnpm add -Dw turbo`
- [x] Create `turbo.json` with pipeline configuration for build, lint, test
- [x] Create directory structure:
  - `apps/web/` (Next.js app)
  - `packages/database/` (Supabase migrations)
  - `packages/ai/` (AI provider abstraction)
  - `packages/ui/` (shadcn/ui components)
  - `packages/types/` (shared TypeScript types)
- [x] Create `package.json` in each package with proper name scoping (`@easyping/database`, etc.)

### Task 2: Setup Next.js App Router (AC: 2)
- [x] Navigate to `apps/web/` directory
- [x] Run `pnpm create next-app@latest .` with options:
  - TypeScript: Yes
  - ESLint: Yes
  - Tailwind CSS: Yes
  - `src/` directory: Yes
  - App Router: Yes
  - Customize import alias: Yes (@/*)
- [x] Verify Next.js 14+ installed in `apps/web/package.json`
- [x] Update `tsconfig.json` to enable strict mode: `"strict": true`
- [x] Create route group directories:
  - `apps/web/src/app/(auth)/` with `layout.tsx`
  - `apps/web/src/app/(dashboard)/` with `layout.tsx`
- [x] Create placeholder pages:
  - `apps/web/src/app/page.tsx` (landing page)
  - `apps/web/src/app/(auth)/login/page.tsx`
  - `apps/web/src/app/(dashboard)/tickets/page.tsx`

### Task 3: Configure Linting and Formatting (AC: 4)
- [x] Install ESLint and Prettier across workspace: `pnpm add -Dw eslint prettier eslint-config-prettier`
- [x] Create root `eslint.config.js` with recommended config (ESLint 9 flat format):
  - Extend `@eslint/js`, `@typescript-eslint`, and `prettier`
  - Rules: `no-unused-vars: error`, `prefer-const: error`, `@typescript-eslint/no-explicit-any: warn`
- [x] Create `.prettierrc` with formatting rules:
  - `semi: true`, `singleQuote: true`, `tabWidth: 2`, `trailingComma: es5`
- [x] Create `.eslintignore` and `.prettierignore` (ignore `node_modules`, `.next`, `dist`)
- [x] Add lint scripts to root `package.json`:
  - `"lint": "turbo run lint"`
  - `"format": "prettier --write \"**/*.{ts,tsx,md}\""`
- [x] Run `pnpm lint` and `pnpm format` to verify setup

### Task 4: Configure Git Hooks with Husky (AC: 5)
- [x] Install Husky and lint-staged: `pnpm add -Dw husky lint-staged`
- [x] Initialize Husky: `pnpm exec husky init`
- [x] Add prepare script to root `package.json`: `"prepare": "husky"` (added by init)
- [x] Create `.husky/pre-commit` hook:
  ```bash
  pnpm exec lint-staged
  ```
- [x] Configure lint-staged in `package.json`:
  ```json
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": ["eslint --fix", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
  ```
- [x] Test pre-commit hook by making a test commit

### Task 5: Setup GitHub Actions CI/CD (AC: 6)
- [x] Create `.github/workflows/` directory
- [x] Create `.github/workflows/ci.yml` with job steps:
  - Checkout code
  - Setup Node.js 18+
  - Install pnpm
  - Install dependencies: `pnpm install`
  - Run lint: `pnpm lint`
  - Run type-check: `pnpm typecheck`
  - Run build: `pnpm build`
- [x] Create `.github/workflows/build.yml` (runs on merge to main):
  - Same steps as ci.yml
  - Additional: Build Docker image (placeholder for now)
- [x] Add `typecheck` script to root `package.json`: `"typecheck": "turbo run typecheck"`
- [x] Add `typecheck` script to each package's `package.json`: `"typecheck": "tsc --noEmit"`
- [x] Add TypeScript as devDependency to all packages that need it
- [x] Verify typecheck works by running `pnpm typecheck`

### Task 6: Configure Performance Budgets (AC: 9)
- [x] Install Next.js bundle analyzer: `pnpm add -D @next/bundle-analyzer`
- [x] Update `apps/web/next.config.ts` with bundle analyzer (TypeScript format):
  ```ts
  import bundleAnalyzer from '@next/bundle-analyzer';
  const withBundleAnalyzer = bundleAnalyzer({
    enabled: process.env.ANALYZE === 'true',
  });
  // Config with optimizePackageImports and onDemandEntries
  export default withBundleAnalyzer(nextConfig);
  ```
- [x] Install Lighthouse CI: `pnpm add -Dw @lhci/cli`
- [x] Create `lighthouserc.js` config file:
  ```js
  module.exports = {
    ci: {
      collect: {
        staticDistDir: './apps/web/.next',
      },
      assert: {
        preset: 'lighthouse:recommended',
        assertions: {
          'categories:performance': ['error', { minScore: 0.9 }],
          'categories:accessibility': ['error', { minScore: 0.95 }],
        },
      },
    },
  };
  ```
- [x] Add performance scripts to `package.json`:
  - `"analyze": "ANALYZE=true pnpm --filter=@easyping/web build"`
  - `"lighthouse": "lhci autorun"`
- [ ] Document performance budgets in README (will be done in Task 8):
  - Bundle size: <500KB gzipped
  - TTI: <2s on 4G
  - Lighthouse Performance: 90+, Accessibility: 95+

### Task 7: Document Coding Standards (AC: 10)
- [x] Create `docs/architecture/coding-standards.md` file
- [x] Document TypeScript standards:
  - Strict mode required
  - No `any` types without `// @ts-expect-error` justification
  - Prefer interfaces over types for object shapes
  - Use explicit return types for functions
- [x] Document naming conventions:
  - Components: `PascalCase` (e.g., `TicketCard.tsx`)
  - Utilities: `camelCase` (e.g., `formatDate.ts`)
  - Constants: `UPPER_SNAKE_CASE` (e.g., `MAX_FILE_SIZE`)
  - Types/Interfaces: `PascalCase` (e.g., `Ticket`, `UserRole`)
- [x] Document file structure:
  - Co-locate tests: `TicketCard.test.tsx` next to `TicketCard.tsx`
  - One component per file
  - Index files for barrel exports only
- [x] Document import organization:
  ```ts
  // 1. External libraries
  import { useState } from 'react';

  // 2. Internal packages
  import { Ticket } from '@easyping/types';

  // 3. Relative imports
  import { formatDate } from '@/lib/utils';
  ```
- [x] ESLint rules already configured in `eslint.config.js`
- [x] Link coding standards from CONTRIBUTING.md

### Task 8: Create README and Environment Template (AC: 7, 8)
- [x] Create root `README.md` with sections:
  - **EasyPing** - Project description and goals
  - **Quick Start** - Installation and setup steps
  - **Development** - How to run locally
  - **Project Structure** - Overview of monorepo layout
  - **Scripts** - Available pnpm commands
  - **Performance Budgets** - Bundle size, TTI, Lighthouse scores
  - **Contributing** - Link to CONTRIBUTING.md
  - **License** - AGPLv3
  - **Roadmap** - Current phase and completed stories
- [x] Create `.env.example` in project root with documented variables:
  - Supabase configuration (URL, anon key, service role key)
  - App configuration (URL, NODE_ENV)
  - AI provider configuration (OpenAI, Anthropic, Azure)
  - Email configuration (SMTP settings)
  - Feature flags (AI, realtime, attachments)
  - Analytics & monitoring (Sentry, PostHog)
  - Development & debugging settings
- [x] Create `apps/web/.env.example` (same content for Next.js app)
- [x] Add instructions in README for copying `.env.example` to `.env.local`
- [x] Document all environment variables with descriptions and example values

### Task 9: Verify Monorepo Works End-to-End
- [x] Run `pnpm install` from root to verify workspace dependencies resolve
- [x] Run `pnpm build` to verify Turborepo pipeline works
- [x] Run `pnpm lint` to verify linting works across all packages
- [x] Run `pnpm typecheck` to verify TypeScript compilation works
- [x] Test git pre-commit hook by staging files and committing
- [x] Verify Next.js app starts: `cd apps/web && pnpm dev`
- [x] Dev server started successfully on http://localhost:3002
- [x] No errors or warnings in console

## Dev Notes

### Previous Story Context
**First Story** - No previous story context.

### Tech Stack Requirements

**[Source: docs/architecture/tech-stack.md]**

This story must configure the following technologies exactly as specified:

- **Frontend Framework:** Next.js 14+ with App Router
- **Language:** TypeScript 5.3+ with strict mode
- **Package Manager:** pnpm 8.0+
- **Build Tool:** Turborepo (latest)
- **Bundler:** Next.js built-in (Turbopack)
- **Linting:** ESLint + Prettier with Husky pre-commit hooks
- **CI/CD:** GitHub Actions
- **CSS Framework:** Tailwind CSS 3.4+
- **Testing:** Vitest (configured in Story 1.8)
- **UI Components:** shadcn/ui + Radix UI (installed in later stories)

**Version Requirements:**
- Node.js 18+
- pnpm 8.0+
- TypeScript 5.3+
- Next.js 14+
- Tailwind CSS 3.4+

### Project Structure to Create

**[Source: docs/architecture/unified-project-structure.md]**

Create the following monorepo structure:

```
easyping/
├── .github/
│   └── workflows/
│       ├── ci.yml
│       ├── build.yml
│       └── release.yml (placeholder for Story 1.5)
├── apps/
│   └── web/
│       ├── src/
│       │   ├── app/
│       │   │   ├── (auth)/
│       │   │   │   └── login/page.tsx
│       │   │   ├── (dashboard)/
│       │   │   │   └── tickets/page.tsx
│       │   │   ├── layout.tsx
│       │   │   ├── page.tsx
│       │   │   └── globals.css
│       │   ├── components/ (empty for now)
│       │   ├── lib/ (empty for now)
│       │   └── hooks/ (empty for now)
│       ├── public/
│       ├── .env.example
│       ├── next.config.js
│       ├── tailwind.config.ts
│       ├── tsconfig.json
│       ├── package.json
│       └── vitest.config.ts (Story 1.8)
├── packages/
│   ├── database/
│   │   ├── package.json
│   │   └── README.md
│   ├── ai/
│   │   ├── src/
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── ui/
│   │   ├── src/
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── types/
│       ├── src/
│       │   └── index.ts
│       ├── package.json
│       └── tsconfig.json
├── docs/
│   ├── architecture/
│   │   └── coding-standards.md (create in this story)
│   └── prd/ (already exists)
├── .husky/
│   └── pre-commit
├── .gitignore
├── .eslintrc.js
├── .prettierrc
├── turbo.json
├── pnpm-workspace.yaml
├── package.json
├── tsconfig.json
├── README.md
├── .env.example
└── LICENSE (AGPLv3, already exists)
```

**Key Decisions:**
- App Router pattern (not Pages Router)
- Route groups: `(auth)` and `(dashboard)` for layout organization
- Shared packages use `@easyping/*` scoping
- Tests co-located with source files
- Monorepo managed with pnpm workspaces + Turborepo

### Coding Standards to Implement

**[Source: AC #10 and CONTRIBUTING.md]**

**TypeScript Configuration:**
```json
{
  "compilerOptions": {
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true
  }
}
```

**ESLint Rules to Enforce:**
```js
{
  "rules": {
    "no-unused-vars": "error",
    "prefer-const": "error",
    "@typescript-eslint/no-explicit-any": "warn",
    "@typescript-eslint/consistent-type-imports": "error"
  }
}
```

**Prettier Configuration:**
```json
{
  "semi": true,
  "singleQuote": true,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100
}
```

**File Naming:**
- React components: `PascalCase.tsx` (e.g., `TicketCard.tsx`)
- Utilities: `camelCase.ts` (e.g., `formatDate.ts`)
- Test files: `*.test.ts` or `*.test.tsx`
- Type files: `types.ts` or `index.ts` in types package

**Import Order (enforced by ESLint):**
1. External libraries (e.g., `import { useState } from 'react'`)
2. Internal packages (e.g., `import { Ticket } from '@easyping/types'`)
3. Relative imports (e.g., `import { utils } from '@/lib/utils'`)

### Performance Budgets to Configure

**[Source: AC #9]**

**Bundle Size:**
- Main JavaScript bundle: <500KB gzipped
- Enforcement: Next.js build warnings + PR comments

**Page Load:**
- Time to Interactive (TTI): <2 seconds on 4G connection
- Enforcement: Lighthouse CI in GitHub Actions

**API Response:**
- Database queries (p95): <500ms
- API endpoints (p95): <2s total
- Enforcement: Manual monitoring initially, automated in post-MVP

**Image Optimization:**
- Max width: 1920px
- Quality: JPEG 85%
- Format: WebP preferred
- Enforcement: Next.js Image component (built-in)

**Asset Optimization:**
- SVG icons: Minified
- CSS: Purged via Tailwind
- Unused imports: Removed by ESLint
- Enforcement: Build process + linting

**Lighthouse CI Thresholds:**
- Performance: 90+ (error if below)
- Accessibility: 95+ (error if below)
- Best Practices: 90+ (warning if below)
- SEO: 90+ (warning if below)

### Configuration Files to Create

**Turborepo Pipeline (`turbo.json`):**
```json
{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "lint": {},
    "typecheck": {},
    "test": {},
    "dev": {
      "cache": false
    }
  }
}
```

**pnpm Workspace (`pnpm-workspace.yaml`):**
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

**Root package.json Scripts:**
```json
{
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "prepare": "husky install"
  }
}
```

### GitHub Actions Workflow Example

**`.github/workflows/ci.yml`:**
```yaml
name: CI

on:
  pull_request:
    branches: [main]

jobs:
  lint-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 8
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm build
```

### Environment Variables Template

**`.env.example` Contents:**
```bash
# Next.js App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase (configured in Story 1.2)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI Provider (optional, configured via UI)
# AI_PROVIDER=openai
# OPENAI_API_KEY=

# Email (optional, configured in Story 1.3)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=

# Monitoring (optional)
# ERROR_WEBHOOK_URL=
```

### Testing

**Testing Standards - Story 1.8**

Testing infrastructure will be configured in Story 1.8. For this story:
- **No unit tests required** (this is infrastructure setup)
- **Manual verification only:** Run through Task 9 verification steps
- **CI/CD validation:** GitHub Actions workflow must pass

Testing standards for future stories (configured in Story 1.8):
- **Test Framework:** Vitest for unit/integration tests
- **Test Location:** Co-locate tests with source files (e.g., `utils.ts` → `utils.test.ts`)
- **Coverage Requirement:** 70% minimum for new code
- **E2E Testing:** Playwright (configured in Story 1.8)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-01-22 | 1.0 | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

**This section will be populated by the Dev Agent during implementation.**

### Agent Model Used

_To be filled by Dev Agent_

### Debug Log References

_To be filled by Dev Agent_

### Completion Notes

_To be filled by Dev Agent_

### File List

_To be filled by Dev Agent_

## QA Results

**This section will be populated by the QA Agent after story completion.**

_To be filled by QA Agent_
